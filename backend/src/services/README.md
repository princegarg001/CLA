Service stubs

This folder is intended to hold provider-specific service modules (e.g. `apollo.js`, `umami.js`, `sentry.js`). The scaffold currently uses inline simple stubs in `routes/api.js` for quick testing. If you'd like, I can expand the services into individual files and implement full API calls for providers where you provide access keys.